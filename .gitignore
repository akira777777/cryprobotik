# Secrets
.env
*.env.local
secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv*/
venv/
env/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# Node (defensive — Mini App is inline Python, but keep ignored in case future frontends are added)
node_modules/
package-lock.json
pnpm-lock.yaml

# IDE
.vscode/
.idea/
*.swp

# Runtime artifacts
logs/
data/
*.sqlite
*.db
*.db-journal
bot.log
bot.log.*
*.log

# Agentic / MCP tool state (local dev only)
.claude-flow/
.kilo/
.kube/
.swarm/
.claude/settings.local.json
.claude/settings.local.*

# MCP server config — per-developer; check in .mcp.example.json instead
.mcp.json

# Docker volumes (when bind-mounted locally)
pgdata/
prometheus_data/
grafana_data/

# OS
.DS_Store
Thumbs.db
